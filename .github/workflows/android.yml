name: Android CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restore stable MainActivity baseline
        run: git show 0d5d1d1ecad5cf9573339596f54b5c6c6f5e7b79:app/src/main/java/com/netftplab/MainActivity.kt > app/src/main/java/com/netftplab/MainActivity.kt

      - name: Apply NetFTP fixes
        run: python3 tools/apply_netftp_fixes.py

      - name: Apply transfer UI fix
        run: python3 tools/apply_transfer_ui_fix.py

      - name: Apply Server UI fix
        run: python3 tools/apply_server_ui_fix.py

      - name: Apply Advanced Monitor UI fix
        run: python3 tools/apply_advanced_monitor.py

      - name: Commit patched source
        if: github.event_name == 'push'
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app/src/main/java/com/netftplab/MainActivity.kt app/src/main/java/com/netftplab/AdvancedNetworkMonitor.kt tools/apply_advanced_monitor.py
          git diff --cached --quiet || git commit -m 'Add advanced network monitor drawer'
          git push origin HEAD:main

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK platform
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Build debug APK
        run: gradle :app:assembleDebug --stacktrace

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: NetFTPLab-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
